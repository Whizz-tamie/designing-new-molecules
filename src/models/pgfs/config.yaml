project: pgfs-td3
entity: whizz
hyperparameters:
  model:
    state_dim: 1024          # Dimension of input state features (e.g., ECFP4 fingerprints)
    template_dim: 102         # Dimension of template features
    action_dim: 1024          # Dimension of action space
    max_action: 1.0          # Max action value
  training:
    gamma: 0.99              # Discount factor
    tau: 0.005               # Soft update rate
    batch_size: 32           # Minibatch size for training
    max_size: 1000000        # Max size for replay buffer
    time_steps: 200000       # Number of timesteps
    max_steps: 5             # Max steps per episode
    policy_freq: 2           # Frequency of policy updates
  optimizers:
    actor_lr: 1e-4           # Learning rate for actor networks
    critic_lr: 3e-4          # Learning rate for critic networks
  temperature:
    initial_temperature: 1.0 # Initial temperature parameter for Gumbel Softmax
    min_temperature: 0.1     # Minimum temperature parameter for Gumbel Softmax
    temp_decay: 0.99         # Temperature decay factor
  noise:
    noise_std: 0.2           # Standard deviation of noise
    noise_clip: 0.5          # Clipping value for noise
  dataset:
    precomputed_vectors_file: '/rds/user/gtj21/hpc-work/designing-new-molecules/data/preprocessed_data/enamine_train_filtered.pkl'
    templates_file: '/rds/user/gtj21/hpc-work/designing-new-molecules/data/preprocessed_data/reactions_R2_filtered.txt'